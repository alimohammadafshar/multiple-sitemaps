The code does the following steps:

It reads the words to search from a text file
It reads the sitemap urls of different websites from another text file
It loops through each sitemap url and extracts all the links from it
It loops through each word and checks if it is in any of the links
It appends the word, link and sitemap url to a list if there is a match
It prints the number of matches found
It saves the list to an excel file